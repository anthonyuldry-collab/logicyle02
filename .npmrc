# Configuration npm — builds CI/Netlify Linux x64
rollup-optional-deps=true
legacy-peer-deps=true

# Ne jamais désactiver la vérification TLS en production / CI.
# (strict-ssl=false a été retiré volontairement)
